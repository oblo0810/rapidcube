[project]
name = "fastcube"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "maturin>=1.12.6",
    "pytest>=9.0.2",
    "pytest-benchmark>=5.1.0",
    "torch>=2.11.0",
]
